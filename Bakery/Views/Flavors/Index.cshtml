@{
  Layout = "_Layout";
}

<h1>Flavors</h1>

@if (@Model.Count == 0)
{
  <h3>No Flavors have been created yet!</h3>
}
else
{
  
}

@if (User.Identity.IsAuthenticated)
{
  <h2>@Html.ActionLink("Add new flavor:", "Create", "Flavors")</h2>
}